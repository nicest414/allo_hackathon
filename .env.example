# mainプロセスのみが読み込む。renderer/preloadには渡さない（src/main/env.ts経由でのみ参照）。
GEMINI_API_KEY=
DEEPGRAM_API_KEY=

# 差し替えポイント: "deepgram" | "gemini_live" (src/main/stt/createSttProvider.ts が参照)
STT_PROVIDER=deepgram
